<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Electron apps with hardened runtime -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required for V8/Chromium) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables (for debugging, can be removed in production) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network access for daemon communication and auto-updates -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access for reading/writing configuration -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Access to user's home directory for ~/.conduit -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
</dict>
</plist>
